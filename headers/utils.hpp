#ifndef	UTILS_HPP
#define	UTILS_HPP


#include <string>
#include <vector>

void    upperIt(std::string &s);
void	skipSpace(std::string &s, size_t &i);
void	trimSpace(std::string &s);
size_t	lastSpace(std::string s);


#endif